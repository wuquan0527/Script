# AI Relay Usage 改动说明（v1.0.2 → v1.2.0）

目标：小组件显示中转站**已用金额**与**请求次数**（原版只显示余额），并加上**今日是否已签到**。
参考：https://github.com/StarYunLee/Scripting/tree/main/AI%20Usage （借用了它的三色预警进度条观感与 label/value 排版）。

## 一、数据来源（已对照 new-api / Veloera 源码核实）

| 指标 | 接口 | 字段 |
|---|---|---|
| 余额 | `GET /api/user/self` | `data.quota` ÷ 500000（额度→美元） |
| **已用金额** | `GET /api/user/self` | `data.used_quota` ÷ 500000 |
| **请求次数** | `GET /api/user/self` | `data.request_count`（new-api 原生的累计次数） |
| 请求次数（回退） | `GET /api/log/self?p=1&page_size=1` | `data.total`（旧版 one-api 无 request_count 时用日志条数兜底） |
| **今日签到（new-api）** | `GET /api/user/checkin?month=YYYY-MM` | `data.stats.checked_in_today`（另含 `checkin_count` / `total_checkins`） |
| **今日签到（Veloera）** | `GET /api/user/check_in_status` | `data.can_check_in`（`false` = 今日已签到） |
| 订阅账单形态 | `/dashboard/billing/subscription` + `/dashboard/billing/usage` | `hard_limit_usd` / `total_usage`÷100 |

已用占比（进度条）：
- 有 `total` 时用 `used / total`；
- New API 只给「余额 + 已用」，用 `used / (used + remaining)` 推导总充值额度（接口语义：`quota`=剩余，`used_quota`=已用）。

签到探测顺序：`/api/user/checkin` → `/api/user/check_in_status`；返回值明确为「签到功能未启用」时标记 `enabled:false`（界面隐藏）；两者都不可用则为 `null`（静默，不影响余额展示）。签到只读查询，不代替用户执行签到（new-api 的 POST 签到需 Turnstile 校验）。

## 二、代码改动

1. `class/api.ts`
   - `BalanceResult` 新增 `requests?: number | null` 与 `checkin?: CheckinInfo | null`（`CheckinInfo` = enabled / checkedToday / monthCount / totalCount / source）；
   - `parseNewApi` 的 `/api/user/self` 形态解析 `request_count`（兼容 `requestCount` / `request_num`）；
   - `/api/usage/token` 与通用模板同步做 best-effort 解析；
   - 新增 `fetchRequestCount()`（日志分页兜底）与 `fetchCheckin()`（双形态签到探测）；
   - `enrichNewApiExtras()` 用 `Promise.all` **并发**补齐次数与签到，避免拖慢小组件时间预算；
   - `getBalance()` 的 newapi 主路径与订阅回退路径均接入该补齐逻辑。
2. `widget/small.tsx`（重写）
   - 主数值为 **余额**，下方三色进度条；
   - 副行：`已用 $x` ↔ `请求 N 次`；末行：**签到徽标** ↔ `更新于 …`；
   - 新增 `formatRequests()`（超万折算为 `x.x万`）、`formatAmountCompact()`、`usedRatio()`、`ratioColor()`、`UsageProgress()`、`checkinLabel()`、`CheckinBadge()`。
3. `widget/medium.tsx`（重写）：余额（含总额）/ 已用（含占比）/ 请求次数 三栏 + 进度条 + 底部签到徽标。
4. `widget/circular.tsx`：可推导占比时显示环形「已用占比」（三色），否则退化为已用金额。
5. `widget/inline.tsx`（锁屏）：`余额 $x · 已用 $y · N 次 · 已签到`。
6. `page/UsagePage.tsx`：卡片新增「已用金额」「请求次数」「今日签到」「本月签到」行 + 进度条。
7. `script.json`：版本 1.2.0、描述更新、**移除 remoteResource**（防止原仓库自动更新把改动覆盖回去）。
   > 若你想保留自动更新，把 `remoteResource` 块填回并把 url 指向你自己的仓库即可。
8. `services/settings.ts`：`APP_VERSION` → `1.2.0`。

## 三、三色预警规则（对齐 AI Usage）

已用占比 ≥85% 红（systemRed）、≥60% 橙（systemOrange）、其余绿（systemGreen）。

## 四、验证

- 用 mock fetch 跑真实 `api.ts`，覆盖 5 组用例 / 14 条断言全部通过：
  new-api 完整形态、旧版 one-api 日志回退 + Veloera 签到、签到未启用、无签到接口、接口无权限，
  并验证「有 request_count 时不会多发日志请求」。

## 五、安装

1. 点开 `dist/AI Relay Usage.scripting` → 分享 → 在 Scripting 中打开（或直接导入）。
2. 重新运行一次脚本刷新缓存（旧缓存没有 requests / checkin 字段，会先显示 `--` 或隐藏签到行）。
3. 小组件会在下次时间线刷新时显示新字段。

## 六、重新打包

```sh
sh /var/minis/workspace/ai-usage/build.sh
```

## 七、已知限制

- OpenRouter 的 `/api/v1/key` 不返回请求次数与签到信息 → 次数显示 `--`、签到行隐藏。
- 旧版 one-api 若关闭了日志查询权限，请求次数同样显示 `--`。
- 站点未启用签到功能时，签到行自动隐藏。
- 上次刷新失败时走本地缓存，缓存里的金额 / 次数 / 签到为上一次成功值。

